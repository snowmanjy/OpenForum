# PLACEHOLDER: Replace with actual public key in PEM format
#
# This file should contain the RSA public key used to verify JWT signatures
# from your parent SaaS platform.
#
# Generate keys with:
#   openssl genrsa -out private-key.pem 2048
#   openssl rsa -in private-key.pem -pubout -out public-key.pem
#
# In production, use environment variable JWT_PUBLIC_KEY_LOCATION to override:
#   JWT_PUBLIC_KEY_LOCATION=file:/path/to/public-key.pem
#
# Or use JWKS endpoint in application.yml:
#   spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://your-auth.com/.well-known/jwks.json
